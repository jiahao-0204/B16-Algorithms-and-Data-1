/**
 * 
 * ? 1.9 Graphs
 * 
 * ? 1.9.1 Shortest paths using the adjacency list representation
 * ? Starting from the code given in the notes, write a version of the 
 * ? Bellman-Ford and Dijkstra algorithms that use the adjacency list 
 * ? representation of a graph instead of the adjacency matrix representation, 
 * ? as given in the notes. In particular, implement the following functions.
 * 
 */