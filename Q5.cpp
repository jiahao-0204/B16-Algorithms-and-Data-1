/**
 * 
 * ? 1.5 Lists
 * 
 * ? 1.5.1 Enhanced list: deleting elements
 * 
 * ? Starting from the module list.hpp given in the notes, write a list_delete_after 
 * ? function adding it to a new list_enhanced.hpp module. Then test it using the 
 * ? following driver.
 */


// ! add "-std=c++14", to arg of tasks.json

/**
 * 
 * list 
 * - fast -> insert and delete
 * - slow -> retreval
 * 
 * vanilla array
 * - fast -> retreval
 * - slow -> insert and delete
 * 
 * 
 */


#include "codes/list_enhanced.hpp"